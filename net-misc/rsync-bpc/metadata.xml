<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pkgmetadata SYSTEM "https://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
    <maintainer type="person">
        <email>vklimovs@gmail.com</email>
        <name>Vjaceslavs Klimovs</name>
    </maintainer>
    <upstream>
        <remote-id type="github">backuppc/rsync-bpc</remote-id>
    </upstream>
    <use>
        <flag name="acl">Preserve and restore POSIX ACLs</flag>
        <flag name="examples">Install contrib helper scripts</flag>
        <flag name="iconv">Support filename charset conversion via iconv</flag>
        <flag name="ipv6">Enable IPv6 support</flag>
        <flag name="static">Build a statically linked binary</flag>
        <flag name="system-zlib">Use system zlib instead of bundled one; incompatible with rsync older than 3.1.1</flag>
        <flag name="xattr">Preserve and restore extended attributes</flag>
    </use>
    <longdescription lang="en">
        Rsync-bpc is a customized version of rsync that is used as part of
        BackupPC. Rsync-bpc is fully line-compatible with vanilla rsync,
        so it can talk to rsync servers and clients. Rsync-bpc serves no
        purpose outside of BackupPC.
    </longdescription>
</pkgmetadata>
